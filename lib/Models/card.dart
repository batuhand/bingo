import 'dart:ui';

class BingoCard{
  String cardName;
  Color cardColor;
  List cardArray;

  BingoCard(String cardName, cardColor, cardArray) {
    this.cardName = cardName;
    this.cardColor = cardColor;
    this.cardArray = cardArray;
  }
}